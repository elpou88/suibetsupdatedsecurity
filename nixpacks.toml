[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]  
cmds = ["corepack enable && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm -r --if-present run build"]

[start]
cmd = "pnpm start"
